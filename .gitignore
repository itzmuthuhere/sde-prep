# dependencies
node_modules/

# build tool output from other stacks (none used here, but ignored per request)
.next/
dist/
build/
out/

# vercel
.vercel/

# editor / OS
.DS_Store
Thumbs.db
*.log
.idea/
.vscode/

# NOTE: content-index.js IS committed on purpose — it is generated by
# `npm run build` but the site must work when served as plain static files
# with no build step.

.vercel
